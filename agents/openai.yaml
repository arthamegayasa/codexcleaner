interface:
  display_name: "Codex Cleaner"
  short_description: "Clean Codex tasks and performance caches safely"
  default_prompt: "Use $codexcleaner to audit both my Codex task history and rebuildable performance caches, then prepare a safe cleanup plan."
