interface:
  display_name: "CodexCleaner Storage Guard"
  short_description: "Audit storage and quarantine reviewed disposable files"
  default_prompt: "Use $codexcleaner to audit Windows storage, preserve projects and outputs, and prepare a reviewed cleanup plan before changing files."
